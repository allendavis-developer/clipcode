{
  "name": "clipcode",
  "version": "0.1.0",
  "private": true,
  "description": "A video editor whose motion graphics are code instead of nodes",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node lint.mjs && node test.mjs && node checks/run.mjs",
    "lint": "node lint.mjs",
    "render": "node render.mjs",
    "checks": "node checks/run.mjs",
    "plan": "node plan.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
